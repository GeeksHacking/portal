// GitHub OAuth callback tests are not included as we use the impersonate API for testing.
// The callback endpoints require a real GitHub OAuth flow which is not testable in integration tests.

namespace HackOMania.Tests.Endpoints.Callback;

// Placeholder - no callback tests needed when using impersonate API
public class CallbackEndpointsTests
{
    // OAuth callback tests would go here if needed, but we use impersonate for auth in tests
}
